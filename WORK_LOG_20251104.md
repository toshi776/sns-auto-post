# 作業ログ - Zenn自動投稿機能の実装

**日付**: 2025-11-04
**作業者**: Claude Code + toshi776

## 📋 実装内容

### 1. Zenn自動投稿システムの完成

Zenn × GitHub連携による自動投稿機能を実装しました。

---

## 🛠️ 実施した作業

### ステップ1: プロジェクト構成の決定
- `D:\DevProjects\sns-auto-post` 配下に `zenn-content` フォルダを作成することを決定
- 既存のD:\Project配下ではなく、プロジェクト内に統合する方針に変更

### ステップ2: Zenn環境のセットアップ
```bash
# zenn-contentフォルダを作成
mkdir zenn-content
cd zenn-content

# npm初期化
npm init -y

# Zenn CLIをインストール
npm install zenn-cli

# 必要なフォルダを作成
mkdir articles

# Gitリポジトリ初期化
git init
```

### ステップ3: .envファイルの更新
`.env`に以下の設定を追加：
```bash
# Zenn（GitHub連携方式）
ZENN_GITHUB_REPO_PATH=D:\DevProjects\sns-auto-post\zenn-content
```

### ステップ4: GitHubリポジトリとの連携
```bash
cd zenn-content

# GitHubリモートリポジトリを追加
git remote add origin https://github.com/toshi776/zenn-content.git

# 初回コミット
git add .
git commit -m "Initial commit: Zenn setup with articles directory"

# ブランチ名をmainに変更
git branch -M main

# GitHubにプッシュ
git push -f origin main

# upstream設定
git push --set-upstream origin main
```

**リポジトリURL**: https://github.com/toshi776/zenn-content

### ステップ5: ZennとGitHubを連携
1. Zennにログイン: https://zenn.dev/
2. ダッシュボード → 「GitHubからのデプロイ」
3. `toshi776/zenn-content` リポジトリを連携

### ステップ6: テスト投稿
```bash
# Dry-runでテスト
python test_zenn_direct.py  # dry_run=True

# 本番投稿
python test_zenn_direct.py  # dry_run=False
```

**結果**:
- ✅ 記事ファイル作成成功: `zenn-python-20251104-093611.md`
- ✅ GitHubへのプッシュ成功
- ✅ Zennへの自動同期開始（数分で反映）

---

## 📁 作成されたファイル

### 新規作成
- `zenn-content/` - Zenn記事管理フォルダ
- `zenn-content/articles/` - 記事ファイル格納フォルダ
- `zenn-content/package.json` - npm設定
- `zenn-content/package-lock.json` - npm依存関係
- `zenn-content/.gitignore` - Git除外設定
- `test_zenn_direct.py` - テスト投稿スクリプト

### 更新されたファイル
- `.env` - ZENN_GITHUB_REPO_PATHを追加

---

## 🎯 完成した機能

### Zenn自動投稿（GitHub連携方式）

#### 使い方1: 統合ファイルで投稿（推奨）
```bash
python main.py --post-file posts/post.txt --zenn-github
```

投稿ファイルの形式（posts/post.txt）:
```
[Zenn Title]
記事のタイトル

[Zenn Content]
記事の本文（マークダウン形式）

複数行OK

[Zenn Emoji]
📝

[Zenn Topics]
Python, 自動化, GitHub
```

#### 使い方2: コマンドライン引数で指定
```bash
python main.py \
  --zenn-title "記事タイトル" \
  --zenn-content "記事本文" \
  --zenn-emoji "📝" \
  --zenn-topics Python GitHub 自動化 \
  --zenn-type tech \
  --zenn-github
```

#### オプション
- `--zenn-github`: GitHub連携方式で投稿
- `--zenn-draft`: 下書きとして保存（公開しない）
- `--zenn-slug`: 記事のスラッグを指定（省略時は自動生成）
- `--dry-run`: 実際には投稿せず、シミュレーションのみ

---

## 📊 システム構成

### 既存のコード
- `main.py:18` - Zenn投稿関数のインポート
  ```python
  from zenn_platform.post_zenn import post_to_zenn
  from zenn_platform.post_zenn_github import post_to_zenn_github
  ```

- `main.py:347-393` - Zenn投稿処理
  - GitHub連携方式: `post_to_zenn_github()`
  - Selenium方式: `post_to_zenn()`

- `zenn_platform/post_zenn_github.py` - GitHub連携実装
  - `generate_slug()`: スラッグ生成
  - `create_article_content()`: マークダウン生成
  - `post_to_zenn_github()`: メイン投稿関数

### 環境変数（.env）
```bash
ZENN_GITHUB_REPO_PATH=D:\DevProjects\sns-auto-post\zenn-content
```

---

## 🔄 自動投稿の仕組み

1. Pythonスクリプト実行
2. 記事マークダウンファイルを生成（フロントマター付き）
3. `zenn-content/articles/` に保存
4. Git add + commit + push
5. GitHubにプッシュ
6. Zennが自動的に同期（数分）
7. 記事が公開される

---

## ✅ 動作確認済み

- [x] Dry-runモードでの動作確認
- [x] 本番投稿の成功
- [x] GitHubへのプッシュ
- [x] Zennとの連携確認

---

## 📍 関連リンク

- **GitHubリポジトリ**: https://github.com/toshi776/zenn-content
- **Zenn記事管理**: https://zenn.dev/dashboard/articles
- **Zennデプロイ設定**: https://zenn.dev/dashboard/deploys

---

## 🚨 注意事項

1. **初回のみ必要な設定**:
   - ZennとGitHubリポジトリの連携（完了済み）
   - upstream branchの設定（完了済み）

2. **記事の公開タイミング**:
   - GitHubにプッシュ後、数分でZennに反映されます
   - すぐには表示されない場合があります

3. **スラッグの重複**:
   - 同じスラッグのファイルが既に存在する場合は上書きされます
   - `--zenn-slug` で明示的に指定するか、自動生成を利用してください

---

## 🎉 完成

Zenn自動投稿システムが完全に動作するようになりました！

今後は `python main.py --post-file posts/post.txt --zenn-github` で簡単にZennに投稿できます。

---

## 📝 メモ

### トラブルシューティング

#### git push失敗（upstream branchエラー）
```bash
cd zenn-content
git push --set-upstream origin main
```
→ 解決済み

#### Zenn CLIの初期化エラー
```bash
npx zenn init
# エラー: ERR_INVALID_FILE_URL_PATH
```
→ 手動で `articles/` フォルダを作成して解決

---

**次回の作業**: この環境を別のマシンでも再現できるように、セットアップ手順を整理する
